# V2_Chapter1_Part1_DBP_Data_Foundation_Codex_Prompt

你现在要协助我完成论文/项目的“第一章第一部分”，主题是：

“基于 SYR4 数据集的 DBP 相关数据基础梳理、环境变量意义解释、跨表可拼接性评估与后续分析层级设计”。

请严格按下面要求执行，不要跳步，不要直接进入机器学习建模，也不要直接做因果分析。当前目标不是追求预测性能，而是建立可信的数据基础、变量体系和分析边界。

## 一、研究背景与任务定位

我当前研究的主线是围绕 SYR4 数据集中的 DBP（消毒副产物）相关数据，后续希望开展机器学习和因果分析。但在进入这些阶段之前，必须先完成第一章第一部分的数据基础工作。

你需要帮助我完成的，不是“直接建模”，而是以下工作：

1. 明确 SYR4 中 DBP 研究范围
2. 梳理各核心变量的环境意义
3. 理清核心数据表之间的关系
4. 评估不同层级下的数据可拼接性与覆盖率
5. 明确后续机器学习和因果分析分别应建立在哪种数据层级上
6. 把这些内容整理为可以直接写进论文/报告的结构化成果

## 二、必须遵守的核心研究判断

请严格以以下判断为前提开展工作：

1. SYR4 的优势在于全国尺度、真实监管场景、多层级结构、结果变量与机制变量并存。
2. SYR4 的劣势在于缺失严重且非随机、监测不是为研究设计、时间不连续、处理信息不完整。
3. 严格样本级四键对齐（PWSID + WATER_FACILITY_ID + SAMPLING_POINT_ID + SAMPLE_COLLECTION_DATE）适合作为“保守基线/可拼接性审计”，但不适合作为后续主建模底表。
4. 后续主分析更可能需要转向：
   - 全国广覆盖主表：PWS-year
   - 高信息机制子样本：PWS-facility-month
5. 机器学习和因果分析不能共用一套口径直接硬做，必须分层设计。
6. 当前阶段的任务是“建立可信数据产品和研究边界”，不是“找最强相关性”。

## 三、当前研究对象范围

请将本阶段分析严格限定在 DBP 主线，不要扩展到 PFAS、砷、总大肠菌群等主题。

请重点围绕以下几类变量与文件：

### 1. 结果变量
- TTHM
- HAA5
- 必要时补充分项 THM/HAA 物种用于理解总量与分项关系

### 2. 前体/反应条件变量
- TOC
- lkalinity
- pH
- DOC
- SUVA
- UV254

### 3. 消毒过程变量
- ree chlorine
- 	otal chlorine
- chloramine（若相关）
- 其他与 residual disinfectant 有关变量

### 4. 结构/背景变量
- PWSID
- SYSTEM_TYPE
- SOURCE_WATER_TYPE
- WATER_FACILITY_ID
- WATER_FACILITY_TYPE
- SAMPLING_POINT_ID
- population served
- 	reatment-related tables / treatment process

## 四、你需要完成的具体任务

请按以下顺序执行，不要跳步。

### 任务 1：梳理“第一章第一部分”的目标与成果结构

先给出一个简明但严谨的说明，回答：
- 第一章第一部分的研究目标是什么
- 它为什么不能等同于建模
- 它最终要产出哪些成果
- 它与后续 ML / 因果分析的关系是什么

请把这部分写成适合论文、开题或研究方案中的正式语言，不要只写口语总结。

### 任务 2：构建 DBP 变量环境意义表

请基于现有 SYR4 数据结构，为我整理一张变量环境意义表，至少包含以下字段：
- 变量名
- 所在数据文件
- 变量类别（结果变量 / 前体变量 / 反应条件 / 消毒过程 / 结构变量）
- 单位
- 环境意义
- 在 DBP 形成中的作用
- 后续适合用于什么分析（描述、预测、机制分析、因果分析等）

要求：
- 不要只停留在字段翻译
- 要把变量和环境过程联系起来
- 对 TOC、DOC、SUVA、UV254、pH、lkalinity、chlorine residual 的环境意义讲清楚

### 任务 3：构建核心数据表关系说明

请系统梳理 DBP 相关核心表的结构和关系，包括但不限于：
- SYR4_THMs
- SYR4_HAAs
- SYR4_DBP_Related Parameters
- SYR4_Disinfectant Residuals
- syr4_treatment 相关表

请说明：
- 每张表一行代表什么
- 主键/候选键是什么
- 与其他表如何连接
- 哪些 join 是严格样本级 join
- 哪些 join 更适合设施-月份级或系统-年份级聚合后再做

请给出一份结构清晰的文字说明，并输出一版适合放进论文的“核心表关系概述”。

### 任务 4：正式审计不同分析层级的数据可用性

这是最关键的部分。

请把数据可用性评估分为 3 个层级：
1. 严格样本级：
   PWSID + WATER_FACILITY_ID + SAMPLING_POINT_ID + SAMPLE_COLLECTION_DATE
2. 设施-月份级：
   PWSID + WATER_FACILITY_ID + year + month
3. 系统-年份级：
   PWSID + year

对每个层级，请围绕以下核心变量做统计：
- TTHM
- HAA5（如果当前阶段先做 TTHM 也可以，但请同时说明 HAA5 应如何纳入）
- pH
- lkalinity
- TOC
- ree chlorine / total chlorine
- 如可行，再补 DOC / SUVA / UV254 的覆盖信息

对每个层级，至少输出：
- 唯一键数
- 非缺失记录数
- 各变量与 TTHM 的交集数
- 多变量共同非缺失的样本数
- 覆盖率变化
- 对该层级是否适合作为后续分析基础的判断

特别要求：
- 不要仅仅输出统计表，要写出解释
- 要明确说明“为什么严格样本级不适合作为主建模底表”
- 要比较“设施-月份级”和“系统-年份级”各自的优缺点

### 任务 5：重新定位我已有的 V1 工作

我已经有一份 V1 严格样本级 Spearman 探究结果。

请你基于前述分析，对这份 V1 工作做正式定位：
- 它的目标应该被定义为什么
- 它的价值是什么
- 它的局限是什么
- 为什么它不能直接升级为主建模数据底表
- 它应该怎样在论文中被表述，避免写偏

请注意：
- 不要简单否定 V1
- 要把它定位成“保守基线 / 样本级可拼接性审计 / sanity check”
- 同时指出它不能直接承担主分析任务

### 任务 6：设计后续数据路线图

请输出一个清晰的数据路线图，区分后续两条主线：

#### A. 全国广覆盖主线（机器学习主表）
建议分析单元：
- PWS-year

请说明：
- 为什么这个层级更适合全国主表
- 应该保留哪些变量
- 应该如何聚合变量（均值、中位数、最大值、样本数、检测率等）
- 它适合回答哪些问题

#### B. 高信息机制主线（机制分析 / 受约束因果分析）
建议分析单元：
- PWS-facility-month

请说明：
- 为什么这个层级适合机制分析
- 应如何进行时间窗口匹配
- 应保留哪些变量
- 它适合回答哪些问题
- 为什么它不能简单外推到全国

### 任务 7：输出论文可直接使用的结构化成果

最终请整理出以下成果：

1. 一版适合写入论文的“第一章第一部分”详细提纲
建议至少包含：
- 研究对象与数据范围
- DBP 相关变量的环境意义
- 核心数据表结构与关系
- 不同分析层级的数据可用性比较
- 第一阶段数据基础结论

2. 一版适合研究执行的“后续工作清单”
要求按步骤列出：
- 先做什么
- 后做什么
- 哪一步的输出是什么
- 哪一步为后续 ML / 因果分析做准备

## 五、工作方式要求

1. 你要优先检查和利用本地已有文件，而不是只做抽象建议。
2. 如果已有 V1 结果文件，请结合其内容进行判断。
3. 输出必须结构清楚、学术表达准确，但不要空泛。
4. 所有判断都要明确说明理由，不要只下结论。
5. 如果需要，先做少量探索再给出结构化方案。
6. 不要直接进入“训练模型”“做特征重要性”“做因果估计”。
7. 当前阶段重点是：
   - 数据基础
   - 分析层级
   - 变量环境意义
   - 可拼接性
   - 后续方法边界

## 六、输出风格要求

请你的输出分成以下几个部分：

1. 第一章第一部分的目标定位
2. 变量环境意义框架
3. 数据表关系框架
4. 三层级数据可用性与方法判断
5. 对 V1 的正式定位与评价
6. 后续 ML / 因果分析的数据路线图
7. 论文提纲
8. 可执行任务清单

如果你认为有必要，可以在执行过程中创建中间表、审计结果和说明文档，但请确保最终给我的内容是可以直接用于论文和项目推进的。

