# V3_最后一个小更新_原始数据映射法则_Codex_Prompt

你现在要协助我完成论文/项目第一章第一部分在 V3 阶段的最后一个小更新任务。

当前阶段不是继续扩展新的统计分析，也不是直接进入机器学习训练，而是要把已经生成的两张 V3 原型主表，正式固定为“可审计、可交接、可进入 V4”的稳定数据产品。

你的任务主题是：

“以简洁直观的方式，固定 V3_facility_month_master 与 V3_pws_year_master 相对于原始 SYR4 数据的变量映射法则、缺失语义、样本分层规则与禁止误用规则。”

请严格按下面要求执行，不要跳步，不要直接建模，不要直接做特征重要性分析，不要直接做因果估计。

## 一、当前前提

请严格以以下前提开展工作：

1. `V3_facility_month_master.csv` 与 `V3_pws_year_master.csv` 已经完成构建，并通过轻量审计。
2. V3 当前的核心任务不是再加新变量，也不是再做新一轮广泛探索，而是固定数据产品契约。
3. 进入 V4 之前，必须先让后续使用者明确知道：每一列从哪来、怎么聚合、空白是什么意思、哪些列不能被误用。
4. 当前项目主线已明确：第一章第一部分的重点是建立可信数据产品和研究边界，而不是直接追求最强相关性。
5. 必须优先利用本地已有 V3 文档、脚本和主表，不要重复造轮子。

## 二、当前任务目标

你需要完成的核心任务是：

1. 为 `V3_facility_month_master` 固定一份面向原始 SYR4 的映射法则说明
2. 为 `V3_pws_year_master` 固定一份面向原始 SYR4 的映射法则说明
3. 用简洁直观的格式，统一说明：
   - 字段来源文件
   - 原始字段
   - 目标层级
   - 聚合函数
   - 单位
   - 空白/缺失的含义
   - 可能的聚合偏差
   - 后续适用分析
4. 固定两张表的样本分层规则
5. 固定两张表的禁止误用规则
6. 输出一份适合后续 V4 直接引用的数据契约式说明文档

## 三、必须依赖的本地文件

你必须优先检查和利用以下文件：

### V3 主表
- `data_local/V3_Chapter1_Part1_Prototype_Build/V3_facility_month_master.csv`
- `data_local/V3_Chapter1_Part1_Prototype_Build/V3_pws_year_master.csv`

### V3 说明文档
- `docs/04_v3/V3_Facility_Month_Dictionary.md`
- `docs/04_v3/V3_Facility_Month_Build_Notes.md`
- `docs/04_v3/V3_PWS_Year_Dictionary.md`
- `docs/04_v3/V3_PWS_Year_Build_Notes.md`
- `docs/04_v3/V3_Prototype_Audit_Report.md`

### V3 脚本
- `scripts/build_v3_chapter1_part1_prototypes.py`
- `scripts/render_v3_prototype_docs.py`

### 原始数据目录
- `D:\Syr4_Project\syr4_DATA_CSV`

## 四、你需要完成的具体任务

请按以下顺序执行，不要跳步。

### 任务 1：明确本次 V3 收尾更新的目标

先用正式但简洁的中文说明：

1. 为什么这一步不等同于建模
2. 为什么在进入 V4 之前必须补这一步
3. 这一步的产出如何服务后续：
   - 描述统计
   - 相关性分析
   - 机器学习
   - 结果解释
   - 论文写作

### 任务 2：为二层主表整理“字段映射总表”

请为 `V3_facility_month_master` 输出一份简洁直观的映射总表，至少包含以下字段：

- V3 字段名
- 字段类别
- 来源原始文件
- 来源原始字段
- 目标层级
- 聚合方式
- 单位
- 缺失值语义
- 聚合偏差/解释风险
- 后续适用分析

特别要求：

- 不要只复制字段字典
- 必须明确它相对于“原始 SYR4 数据”是如何得到的
- 要优先覆盖以下字段组：
  - 结果变量
  - 核心机制变量
  - 扩展机制变量
  - 结构背景变量
  - treatment 摘要字段
  - 质量控制字段

### 任务 3：为三层主表整理“字段映射总表”

请为 `V3_pws_year_master` 输出同样格式的映射总表，至少包含：

- V3 字段名
- 字段类别
- 来源原始文件
- 来源原始字段
- 中间来源（二层字段，如适用）
- 目标层级
- 聚合方式
- 单位
- 缺失值语义
- 聚合偏差/解释风险
- 后续适用分析

特别要求：

- 必须明确哪些字段是“从二层再上卷”，不是直接来自原始 occurrence
- 必须明确像 `*_sample_weighted_mean*`、`*_monthly_max_max*`、`*_high_risk_*` 这些字段到底是什么含义

### 任务 4：固定缺失值语义

请专门写一个简洁章节，说明两张 V3 主表中空白值可能分别代表什么。

至少区分以下几种情况：

1. 原始数据未监测
2. 原始有记录但没有可用数值
3. 该系统/设施/年份不在该变量的覆盖结构中
4. 聚合后没有有效值

请明确说明：

- 这些空白不能简单视为普通随机缺失
- 后续机器学习中如何理解这些空白

### 任务 5：固定样本分层规则

请把当前 V3 阶段已经形成的样本分层，用正式语言固定下来。

至少包括：

#### 对第三层 `V3_pws_year_master`
- 全国主模型样本
- 加强模型样本
- 高信息模型样本

#### 对第二层 `V3_facility_month_master`
- 机制分析起点样本
- 更高信息的小模型子样本

要求：

- 给出明确筛选条件
- 给出这样分层的理由
- 说明这些分层如何服务后续 V4

### 任务 6：固定禁止误用规则

请写一节“禁止误用规则”，至少明确：

1. 不能把二层主表误写成全国统一全变量宽表
2. 不能把目标同源摘要字段直接送回模型作为特征
3. 不能把字符串摘要字段直接原样入模
4. 不能把极度稀疏扩展变量默认纳入第一版全国主模型
5. 不能把三层聚合字段误解为原始样本值
6. 不能把空白值简单当作普通随机缺失

### 任务 7：输出最终文档

请最终生成一份新的中文 Markdown 文档，建议命名风格类似：

- `docs/04_v3/V3_Raw_Data_Mapping_Rules.md`
或
- `docs/04_v3/V3_Master_Data_Contract.md`

文档必须包含以下部分：

1. 本次 V3 收尾更新的目标说明
2. 二层主表映射总表
3. 三层主表映射总表
4. 缺失值语义说明
5. 样本分层规则
6. 禁止误用规则
7. 进入 V4 前的准备结论

## 五、工作方式要求

1. 优先使用本地已有 V3 文档与脚本，不要重复造轮子。
2. 输出要简洁、直观、可查，不要写成冗长散文。
3. 这次更新的重点是“固定规则”，不是“扩展分析”。
4. 所有判断都要给出理由，不要只下结论。
5. Markdown 文档必须使用中文撰写。
6. 更新完成后，请同步更新 `codex.md`。

## 六、最终输出要求

请最终给出：

1. 你新生成的 V3 映射法则文档路径
2. 文档中最关键的结论摘要
3. 这一步为什么是进入 V4 之前必要的准备
4. 对下一步是否进入 V4 的明确判断

如果需要，可以在执行过程中创建中间表或中间说明，但最终必须留下一个可直接复用的、简洁直观的 V3 收尾文档。

