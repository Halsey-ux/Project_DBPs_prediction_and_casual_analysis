# V3_Chapter1_Part1_Prototype_Build_Codex_Prompt

你现在要协助我完成论文/项目第一章第一部分的 V3 阶段任务。

当前阶段不是继续做抽象讨论，也不是直接进入机器学习训练或因果估计，而是要把前面已经完成的 V2 数据基础审计结果，真正落地成第二层和第三层的原型分析表。

你的任务主题是：

“基于 V2 审计结论，构建 SYR4 的第二层 PWS-facility-month 原型表与第三层 PWS-year 原型表，并完成字段规则、聚合逻辑和轻量验证。”

请严格按下面要求执行，不要跳步，不要直接训练模型，不要直接做特征重要性分析，不要直接做因果估计。

## 一、当前前提

请严格以以下前提开展工作：

1. 第一层 strict_sample 已证明不适合作为后续主建模底表，只保留为保守基线和可拼接性审计层。
2. 第二层 acility-month 已被证明可行，但不能假设存在完整多变量宽表；更适合模块化变量集、时间窗口约束、pairwise 或小模型策略。
3. 第三层 PWS-year 已被证明可行，且更适合作为全国机器学习主表原型；但仍应采用简约变量集和聚合统计。
4. 当前阶段目标不是追求最终性能，而是产出两张真正可用的原型底表。
5. 必须优先利用本地已有 V2 审计结果、模板文件和既有目录结构。

## 二、当前任务目标

你需要完成的核心任务是：

1. 设计并固定第二层原型表 V3_facility_month_master 的字段方案
2. 设计并固定第三层原型表 V3_pws_year_master 的字段方案
3. 从 SYR4 原始表逐表构建第二层摘要表
4. 合并生成第二层原型表
5. 从第二层进一步聚合生成第三层原型表
6. 输出字段字典、聚合规则说明和轻量审计结果
7. 判断第二层和第三层原型表是否足以支持后续机制分析和全国机器学习主表建设

## 三、变量范围要求

当前阶段分析严格限定在 DBP 主线。

### 结果变量
- TTHM
- HAA5

### 核心机制变量
- pH
- TOTAL ALKALINITY
- TOTAL ORGANIC CARBON
- FREE RESIDUAL CHLORINE (1013)

### 扩展机制变量
- TOTAL CHLORINE (1000)
- DOC
- SUVA
- UV254
- CHLORAMINE（如适用）

### 结构背景变量
- PWSID
- SYSTEM_TYPE
- SOURCE_WATER_TYPE
- WATER_FACILITY_ID
- WATER_FACILITY_TYPE
- 人口字段
- treatment 相关表中的设施与工艺摘要信息

## 四、必须遵守的方法原则

1. 不要直接在原始 TTHM 样本表后面横向硬拼所有变量。
2. 必须先对每张源表分别聚合到目标层级，再按统一主键合并。
3. 第二层键必须是：
   - PWSID + WATER_FACILITY_ID + year + month
4. 第三层键必须是：
   - PWSID + year
5. 对浓度类变量，不要简单求和；应优先使用：
   - count
   - mean
   - median
   - max
   - 必要时 p90
6. 对计数或结构类变量，才可使用 count 或 sum 等聚合。
7. 必须保留质量控制字段，如：
   - 
_core_vars_available
   - match_quality_tier
   - months_with_data
   - 
_facilities_with_tthm

## 五、你需要完成的具体任务

请按以下顺序执行，不要跳步。

### 任务 1：固定第二层和第三层的字段清单

请基于当前项目已有模板和 V2 审计结果，确定：

#### 第二层 V3_facility_month_master
至少应包含：
- 主键字段
- 结果变量月度聚合字段
- 核心机制变量月度聚合字段
- 扩展机制变量月度聚合字段
- 结构背景字段
- treatment 摘要字段
- 质量控制字段

#### 第三层 V3_pws_year_master
至少应包含：
- 主键字段
- 年度结果变量字段
- 年度机制变量字段
- 系统结构字段
- treatment 摘要字段
- 质量控制字段

请明确说明每个字段为何保留，以及它属于哪一类字段。

### 任务 2：逐表构建第二层摘要表

请分别从相关源表生成第二层摘要表，例如：
- 	thm_facility_month
- haa5_facility_month
- ph_facility_month
- lkalinity_facility_month
- 	oc_facility_month
- ree_chlorine_facility_month
- 	otal_chlorine_facility_month
- 如可行，再生成 doc/suva/uv254_facility_month

每张摘要表至少输出：
- 唯一键数
- 非缺失键数
- 用到的聚合函数
- 关键字段示例

### 任务 3：合并生成第二层原型表

请将第二层各摘要表按统一主键合并，生成：
- V3_facility_month_master.csv

同时生成：
- V3_facility_month_dictionary.md
- V3_facility_month_build_notes.md

要求：
- 给出 merge 逻辑
- 说明哪些字段来自哪张源表
- 明确哪些字段是结果变量、哪些是机制变量、哪些是结构字段

### 任务 4：从第二层进一步聚合出第三层原型表

请从第二层表进一步聚合生成：
- V3_pws_year_master.csv

同时生成：
- V3_pws_year_dictionary.md
- V3_pws_year_build_notes.md

要求：
- 明确从第二层向第三层聚合的函数和逻辑
- 说明为什么这些年度聚合统计适合后续全国机器学习主表

### 任务 5：做 V3 轻量审计

请对两张 V3 原型表分别输出轻量审计结果，至少包括：
- 行数
- 主键唯一性检查
- 核心变量覆盖率
- 与 V2 审计结论是否一致
- 第二层是否已足够作为机制分析起点
- 第三层是否已足够作为全国主表原型

请将结果写入：
- V3_prototype_audit_report.md

### 任务 6：给出 V3 阶段结论与下一步建议

请最终明确回答：
1. 第二层原型表是否已经足够支持后续高风险场景内部分析和小模型机制分析
2. 第三层原型表是否已经足够支持后续全国机器学习主表建设
3. 下一步应优先进入哪条线：
   - 第三层全国 ML 主表线
   - 第二层机制 / 风险场景线

## 六、工作方式要求

1. 优先使用本地已有文件与目录，不要重复造轮子。
2. 如果已有 V2 报告、模板 Excel 或字段设计文件，请结合使用。
3. 输出必须结构清楚、字段命名一致、便于后续复用。
4. 所有判断都要给出理由，不要只下结论。
5. 当前阶段重点是：
   - 原型表构建
   - 字段设计
   - 聚合逻辑
   - 轻量验证
   - 下一步方法分流
6. 不要直接进入训练模型、特征重要性分析或因果估计。

## 七、最终输出要求

请把最终输出整理为以下几个部分：

1. V3 工作目标说明
2. 第二层原型表字段方案
3. 第三层原型表字段方案
4. 第二层构建过程与结果
   - 必须实际生成第二层原型表文件 V3_facility_month_master.csv
   - 必须说明第二层主键、字段数、行数、变量来源、聚合方式，并展示示例记录
5. 第三层构建过程与结果
   - 必须实际生成第三层原型表文件 V3_pws_year_master.csv
   - 必须说明第三层主键、字段数、行数、变量来源、聚合方式，并展示示例记录
6. V3 轻量审计结论
   - 必须基于已生成的第二层和第三层原型表，而不是只做抽象讨论
   - 必须明确判断这两张原型表是否已足够支持后续机制分析与全国机器学习主表建设
7. 下一步建议

如果需要，请在执行过程中创建中间摘要表和说明文档，但最终必须留下可直接复用的 V3 原型表与文档。

